using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CountConsoles : MonoBehaviour
{
    static public bool Console1_Working =false;
    static public bool Console2_Working = false;
    static public bool Console3_Working = false;
    static public bool Console4_Working = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
